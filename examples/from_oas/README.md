## `from_oas`

Examples that **import** OAS specs (YAML) and recreate runnable workflows/agents with `dapr run`.

The YAMLs are generated by the examples in `../to_oas/`.

Each example directory contains a `dapr.yaml` template. Run it with `dapr run -f <example-dir>`.
